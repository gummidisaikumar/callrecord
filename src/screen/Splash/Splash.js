import React, {useState, useContext} from 'react';
import {
  View,
  ScrollView,
  KeyboardAvoidingView,
  Platform,
  Text,
  Button,
  TouchableOpacity,
} from 'react-native';


const Splash = () => {
  return (
    <View>
        <Text>loading...</Text>
    </View>
  );
};

export default Splash;
